import React, {useState} from "react";
import Input from "../common/Input"

const Exchange = () => {
    return (
        <React.Fragment>
            <Input />
        </React.Fragment>
    );
}

export default Exchange;